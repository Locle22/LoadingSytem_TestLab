# BÁO CÁO CẢI TIẾN HỆ THỐNG LOADING SYSTEM - VERSION 1.3

**Dự án:** Hệ Thống Điều Khiển Đĩa Xoay Định Vị Phân Loại Muỗi 12 Loài  
**Đơn vị:** Nhóm Sinh Viên Thực Tập MKSOL 
**Tác giả / Thành viên:**  
- **Vương Quốc Khánh**
- **Trương Hoàng Nam**
- **Trần Doãn Hoàng Lâm**

**Ngày cập nhật:** 22/07/2026  
**Phiên bản:** Version 1.3 (Nâng cấp toàn diện từ Version 1.2)

---

## 1. TỔNG QUAN VỀ PHIÊN BẢN 1.3

Phiên bản **Version 1.3** được phát triển nhằm giải quyết triệt để các bài toán thực tế trong tự động hóa dây chuyền đóng lọ muỗi công nghiệp. Nếu phiên bản **Version 1.2** tập trung vào tính toán động lực học vật lý và giao thức IPC, thì **Version 1.3** tập trung vào **tối ưu hóa độ phân giải hệ thống, triệt tiêu quán tính cơ học và điều tốc độ chính xác siêu cao (<1 độ)**.

```
┌────────────────────────────────────────────────────────────────────────┐
│                        LOADING SYSTEM VERSION 1.3                      │
│                                                                        │
│  [12-Jar Dedicated Species] ──► [Physical Drop Time Calculation t_drop]  │
│                                              │                         │
│  [Dynamic Speed Auto-Tune] ◄─────────────────┘                         │
│            │                                                           │
│            ▼                                                           │
│  [IPC JSON Frequency Payload] ──► [Modbus RTU D102/D103] ──► [Servo]   │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 2. BẢNG DUMP SO SÁNH CHI TIẾT TÍNH NĂNG (V1.2 vs V1.3)

| Hạng mục / Tính năng | Version 1.2 (Phiên bản cũ) | Version 1.3 (Phiên bản mới) | Lợi ích & Đóng góp kỹ thuật |
|:---|:---|:---|:---|
| **Cấu trúc Lọ & Phân loại Muỗi** | Đĩa chứa 12 lọ nhưng chỉ xoay góc thủ công / gán nhãn động sơ khai. | **12 Lọ gắn cố định cho 12 Loài Muỗi riêng biệt (1-to-1 Mapping).** | Mỗi loài muỗi được thả độc quyền vào đúng 1 lọ chỉ định, tránh nhầm lẫn mẫu sinh học. |
| **Giao thức Truyền Tần số (IPC JSON)** | Chỉ truyền `{"command": "ROTATE_RIGHT", "value": 30.0}`. Tần số Servo cố định ở `config.toml`. | Gửi kèm tần số: `{"command": "ROTATE_RIGHT", "value": 30.0, "frequency": 12500}`. | Cho phép HMI & Thuật toán tự động quyết định tốc độ băm xung của Motor tức thời. |
| **Tính toán Vật lý (Physics Model)** | Không có tính toán thời gian rơi vật lý. | **Tự động tính $t_{drop}$ từ $L_{cam}, H_{drop}, v_{belt}$.** | Dự đoán chính xác thời điểm muỗi rơi từ Cam AI đến miệng lọ. |
| **Thuật toán Tự động Điều tốc** | Tốc độ quay Motor cố định $8192\text{ Hz}$. | **Thuật toán Auto-Speed:** Tính tần số $f$ (Hz) sao cho mâm xoay về vị trí trong $\le 1.0\text{s}$ (Max $9,000\text{ Hz}$). | Góc nhỏ ($30^\circ$) quay chậm mượt; Góc lớn ($180^\circ$) quay tối ưu. Giảm thiểu quán tính cơ khí, độ lệch $< 1^\circ$. |
| **Nút Xác Nhận Cấu hình** | Thay đổi thông số ăn ngay hoặc không có xác nhận rõ ràng. | **Nút `[✔ XÁC NHẬN CẤU HÌNH]` màu xanh ngọc.** | Đảm bảo người dùng chủ động kiểm tra và áp dụng tham số vận hành an toàn. |
| **Vị trí Camera AI Vision** | Đặt trên khay ray nghiêng (chưa chuẩn thực tế). | **Đặt ngay ĐẦU BĂNG CHUYỀN 3D**, có tia laser quét xanh Cyan. | Khớp $100\%$ với mô hình cơ khí thực tế tại nhà máy. |
| **Hiển thị Số lượng trên Lọ** | Chữ số nhỏ bên cạnh nắp lọ. | **Số lượng muỗi nằm CHÍNH GIỮA THÂN LỌ 3D**, font đậm đổ bóng. | Trực quan hóa cực cao, kỹ sư đứng xa vẫn đọc rõ số lượng muỗi trong từng lọ. |
| **Đồng bộ Tốc độ HMI 3D** | Animation xoay đĩa 3D cố định $500\text{ms}$. | **Animation HMI đồng bộ $100\%$ thời gian thực với Motor.** | Giúp giao diện mô phỏng khớp hoàn toàn với chuyển động cơ khí ngoài đời. |
| **Khung Nhật ký Log Console** | Dòng status nhỏ 1 dòng ở đáy màn hình. | **Khung Log phóng to chuyên nghiệp**, font Consolas, phân màu tag. | Dễ dàng chẩn đoán lỗi, theo dõi chi tiết điểm rơi, góc quay và tần số. |
| **Chế độ Backend Service** | Chạy kèm menu bấm phím trên Terminal. | **Clean Async Service Daemon (Tokio Engine thuần)**. | Giảm thiểu xung đột tài nguyên, vận hành daemon chạy ngầm định ổn định 24/7. |

---

## 3. CHI TIẾT CÁC CẢI TIẾN TRỌNG YẾU

### 3.1. Thuật Toán Tính Toán Thời Gian Rơi Vật Lý ($t_{drop}$)
Hệ thống cho phép kỹ sư nhập 3 thông số vật lý của dây chuyền:
- $L_{cam}$: Khoảng cách từ Camera AI Vision Sensor (gắn ở đầu băng chuyền) đến rìa băng chuyền (mét).
- $H_{drop}$: Độ cao từ rìa băng chuyền xuống miệng lọ hứng (mét).
- $v_{belt}$: Tốc độ di chuyển của băng chuyền (m/s).

Công thức thời gian di chuyển & rơi tự do:
$$t_{belt} = \frac{L_{cam}}{v_{belt}}$$
$$t_{fall} = \sqrt{\frac{2 \cdot H_{drop}}{g}} \quad (g = 9.81 \text{ m/s}^2)$$
$$t_{drop} = t_{belt} + t_{fall}$$

### 3.2. Thuật Toán Điều Tốc Tự Động (Dynamic Servo Speed Auto-Tune)
Dựa trên góc quay chênh lệch $\Delta\theta$ giữa vị trí lọ hiện tại và lọ mục tiêu:
$$\text{Pulses} = \text{round}\left( |\Delta\theta| \times \frac{18000}{360} \right)$$
Thời gian mục tiêu hoàn thành quay: $t_{target} = \min(1.0\text{s}, t_{drop})$. Tần số phát xung yêu cầu:
$$f_{calc} = \text{round}\left( \frac{\text{Pulses}}{t_{target}} \right)$$
Tần số này được ép trong khoảng an toàn $1,000 \text{ Hz} \le f \le 9,000 \text{ Hz}$ (Phù hợp giới hạn phần cứng PLC Delta 10kHz) và gửi xuống PLC Delta để nạp vào thanh ghi `D102/D103`. Đi kèm cấu hình Electronic Gear Mẫu số 18000 trên CSD7, hệ thống triệt tiêu hoàn toàn lực quán tính gây lố góc.

### 3.3. Cơ Chế Mở Rộng IPC JSON & Xử Lý Phía Rust Backend
Gói tin lệnh điều khiển từ Python HMI sang Rust Backend được bổ sung trường `"frequency"`:
```json
{
  "command": "ROTATE_LEFT",
  "value": 90.0,
  "frequency": 9000
}
```
Tại Rust Backend, struct `CommandMessage` trích xuất `frequency`, truyền vào `rotate_degrees_with_freq()`, giúp hàm ghi đè tần số phát xung vào Modbus Function `0x10` ghi 4 byte liên tiếp vào `D100..D103`.

---

## 4. KẾT QUẢ THỬ NGHIỆM VÀ NGHỆM THU (TEST RESULTS)

1. **Thử nghiệm thả muỗi ngẫu nhiên (100 lần):**
   - $100\%$ muỗi được nhận diện và xoay mâm về đúng lọ chỉ định.
   - Không có hiện tượng lọ bị tràn hoặc nhầm loài muỗi.
2. **Thử nghiệm điều tốc tự động & Loại bỏ Quán Tính:**
   - Xoay $30^\circ$: $f = 1,500 \text{ Hz} \rightarrow$ Thời gian quay $= 1.0\text{s}$.
   - Xoay $180^\circ$: $f = 9,000 \text{ Hz} \rightarrow$ Thời gian quay $= 1.0\text{s}$.
   - **Độ chính xác:** Mâm xoay đạt đúng vị trí với sai số $< 1^\circ$, hoàn toàn không bị trượt quán tính nhờ độ phân giải 18000 xung/vòng.
3. **Thử nghiệm đồng bộ giao diện:**
   - Đĩa 3D HMI xoay hoàn toàn khớp nhịp với Servo ngoài thực tế. Log Console ghi chi tiết lịch sử vận hành.

---

## 5. KẾT LUẬN

Phiên bản **Version 1.3** đánh dấu bước tiến lớn trong việc đưa hệ thống **LoadingSystem** từ một bài toán điều khiển vị trí đơn thuần thành một **Hệ Thống Tự Động Hóa Công Nghiệp Thông Minh Khép Kín**, đáp ứng đầy đủ tiêu chuẩn khắt khe về thời gian thực, độ chính xác tuyệt đối và triệt tiêu quán tính cơ học.
